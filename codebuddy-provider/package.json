{
  "name": "codebuddy-provider",
  "displayName": "CodeBuddy Model Provider",
  "description": "Register CodeBuddy (Tencent) models as a VS Code language model provider, usable inside Copilot Chat / Agent.",
  "version": "0.0.1",
  "publisher": "Lonze",
  "private": true,
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ilonze/Xmart.VSCode.AiModels.CodeBuddy.git"
  },
  "engines": {
    "vscode": ">=1.125.0"
  },
  "enabledApiProposals": [
    "chatProvider"
  ],
  "categories": [
    "AI",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "languageModelChatProviders": [
      {
        "vendor": "codebuddy",
        "displayName": "CodeBuddy (Tencent)"
      }
    ],
    "configuration": {
      "title": "CodeBuddy Provider",
      "properties": {
        "codebuddy.accessToken": {
          "type": "string",
          "default": "",
          "secret": true,
          "markdownDescription": "CodeBuddy access token (from the CodeBuddy console/backend or the official plugin/CLI login). Sent as `Authorization: Bearer <token>`. When empty, the `CODEBUDDY_AUTH_TOKEN` environment variable is used."
        },
        "codebuddy.userId": {
          "type": "string",
          "default": "",
          "markdownDescription": "CodeBuddy account uid, sent as `X-User-Id`. When empty, the request falls back to `X-No-User-Id: 1`."
        },
        "codebuddy.modelsCacheTtlSeconds": {
          "type": "number",
          "default": 1800,
          "minimum": 30,
          "markdownDescription": "How long (in seconds) to keep the fetched model list before refreshing it automatically. Minimum 30. Use the `CodeBuddy: Refresh Models` command to refresh immediately."
        }
      }
    },
    "commands": [
      {
        "command": "codebuddy.refreshModels",
        "title": "Refresh Models",
        "category": "CodeBuddy"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.125.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
