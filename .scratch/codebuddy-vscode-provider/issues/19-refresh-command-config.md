# 19 — 刷新命令与 TTL 配置接线

**What to build:** 用户在命令面板可执行"刷新模型清单"命令,立即强制刷新(绕过 TTL);模型缓存 TTL 可通过设置项调整(默认 30 分钟),注册表按配置值工作。

**Blocked by:** 17(Model Registry)

**Status:** done

- [x] "刷新模型清单"命令存在并触发强制刷新
- [x] TTL 设置项存在(默认 1800 秒),注册表读取并按配置工作
- [x] F5 手动验证命令与设置生效
